{
  "version": 2,
  "builds": [
    {
      "src": "Dockerfile",
      "use": "docker",
      "config": {
        "dockerfile": "Dockerfile"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
